---
import Head from './partials/_head.astro'
import TopNav from './partials/_topbar.astro'
import QuickLinks from './partials/_quick-links.astro'
import Footer from './partials/_footer.astro'
import Scripts from './partials/_scripts.astro'

interface Props {
  title: string
  description?: string
  currentPath?: string
  pageScript?: string
  pageCss?: string
  isRtl?: boolean
}

const { title, description, pageScript, pageCss, isRtl = false } = Astro.props

const pageSeo = {
  title,
  description: description || `${title} page of the admin dashboard`
}
---

<!doctype html>
<html lang="en" dir={isRtl ? 'rtl' : 'ltr'} data-bs-theme="light">
  <head>
    <Head {...pageSeo} {pageCss} isRtl={isRtl} />
  </head>
  <body>
    <!-- Main Content -->
    <main>
      <TopNav />
      <!-- Page Content - No wrapper containers for full-width sections -->
      <slot />
      <QuickLinks />
      <Footer />
    </main>
    <!-- All Scripts -->
    <Scripts {pageScript} />
  </body>
</html>
